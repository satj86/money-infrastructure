version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: bash create-acr.sh
      - run:
          name: Print the Current Time
          command: date